# Next Tasks

## Current Status

The repository is public and domain-linked to `ref.arcbos.com`, but the inspected root is minimal. `README.md` is nearly empty, `CNAME` exists, and no root `index.html` or `package.json` was found during inspection.

## Next Recommended Tasks

1. Verify GitHub Pages settings and confirm the actual publishing source for `ref.arcbos.com`.
2. Inspect the full repository tree to identify whether content exists outside the root files checked in this session.
3. Improve `README.md` with the repository purpose, domain, structure, and safe editing rules.
4. Confirm whether this repository should contain public ARCBOS reference docs, ARCBOS VI, product documentation, or redirect content.
5. If public HTML is added, include responsive layout, social preview metadata, favicon, and preview image.
6. Confirm whether `ref.arcbos.com` is the long-term canonical domain.

## Do Not Do

- Do not add dependencies.
- Do not add `package.json`.
- Do not change `CNAME` without explicit approval.
- Do not change public routes without explicit approval.
- Do not invent a site architecture before verifying the intended publishing model.
- Do not expose private ARCBOS engineering, supplier, investor, or credential information.
- Do not use toy-like startup visuals.

## Handoff Prompt

Continue this repository from its project context.

First read:
1. CLAUDE.md
2. docs/project-brief.md
3. docs/decision-log.md
4. docs/todo-next.md
5. docs/acceptance-checklist.md

Then summarize the current state in no more than 8 bullets and execute only the next task listed in docs/todo-next.md. Do not change scope.
