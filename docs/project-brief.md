# Project Brief

## Repository

enxpower/docs-arcbos-v2

## Purpose

This repository appears to provide public ARCBOS reference documentation or reference material served from `ref.arcbos.com`.

## Public / Private Status

The repository is public and appears public-facing because it contains a root `CNAME` pointing to `ref.arcbos.com`.

## Current Known Structure

- `README.md` — minimal repository title and `ref` marker.
- `CNAME` — custom domain configuration for `ref.arcbos.com`.

## Deployment / Runtime

Likely GitHub Pages static hosting because the repository has a root `CNAME`. No root `index.html` or root `package.json` was found during inspection. The actual publishing source and site generation path are To verify.

## Related Brand / Domain

- ARCBOS
- `ref.arcbos.com`

## Important Constraints

- Treat this as a public ARCBOS reference surface.
- Do not change `CNAME` or public domain routing without explicit approval.
- Do not add a framework, dependency, build system, or package file without explicit approval.
- Use ARCBOS VI for any public pages or visual documentation.
- Keep future public presentation practical, industrial, technical, and execution-focused.
- Do not expose credentials, internal project data, supplier information, or private implementation logic.

## What Future AI Agents Must Understand

- The current inspected root is minimal; do not assume a full site architecture without further inspection.
- `ref.arcbos.com` is the only verified domain signal from the repository.
- The repository likely needs a proper README and structure map before larger documentation work.
- If public HTML is added later, it must include responsive layout, relevant metadata, favicon, and preview image.
- Do not make broad structural changes until the intended publishing model is confirmed.
